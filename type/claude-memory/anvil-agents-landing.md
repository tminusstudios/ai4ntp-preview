---
name: anvil-agents-landing
description: The public Agents-as-a-Service sales page at ai4ntp.com/agents (rebranded off "Anvil" Jul 8; major restructure + build-lane pricing Jul 11 2026)
metadata:
  type: project
  originSessionId: 3eedde69-c626-458e-8aaa-7c9a36e7f091
---

The public Agents-as-a-Service sales page is live at **ai4ntp.com/agents**. **On July 8, 2026 it was rebranded off the "Anvil" sub-brand into the pure AI4NTP master brand** at Justin's request (he wanted brand consistency, not a page that "feels like a separate thing"). The offering is now presented as **"Your AI Department"** (AI4NTP's on-demand AI department) using the `ai4ntp` wordmark. **No more "Anvil", no forge/anvil metaphor, no "This is the way." motif.**

**Source:** `agents/index.html` (still a single self-contained dark page, but now re-skinned to the AI4NTP brand kit, NOT the old generic dark-SaaS look). Approach was a token re-skin in place, not a rewrite to the v6 classes: its own inline `:root` was recolored (lime `#D4FF3A` single accent, teal+amber collapsed into lime; ink `#0F1113`; rust for urgency), fonts swapped to Fraunces/Instrument Serif/JetBrains Mono/Hanken via a Google Fonts `<link>`, all `border-radius` -> 0 (square geometry), grain `body::before` overlay added (mix-blend-mode:overlay @0.14 on ink; multiply is invisible on dark), topbar/footer wordmark = `ai<span class="four">4</span>ntp`. It now loads the shared **`/footer.js`** (inline Anvil footer deleted) and **`/ref.js`**. See [[ai4ntp-dark-theme-system]] for why linking base.css/dark.css was rejected (cascade conflicts with the page's own class names).

**Offer = Hormozi Grand Slam, kept intact but made honest.** Same flow (shift -> in-house-vs-monthly math -> value stack -> 4 bonuses incl. "bonus of the bonus" price lock -> 110% first-build guarantee -> why-us -> what-we-build -> how-it-works -> pricing box -> FAQ -> close). Key July-8 copy changes: hero no longer leads with the $500k/$5k anchor (that anchoring stays in the "math" section only); every "book a fit call" CTA became **"Apply"** with exclusive/founding-cohort framing; removed the fabricated "$2k-$10k/mo" token value (now "Included"); softened "tokens on us, you never see a bill ever" -> "we cover tokens/compute on the vast majority of builds, ~90% on local models, we warn you up front on heavy compute"; removed the fixed "about a week / one build a week" cadence everywhere -> "some builds under 48h, others over a week, you know the timeline once we scope it."

**Offer terms (SUPERSEDED July 11, see below):** was $5,000/mo doubling to $8,000 on Aug 15 2026, 10 seats / 4 left, ICP spelled as "$1M-$50M". All of that scarcity/anchor framing and the $1M-$50M number were REMOVED in the July 11 rework.

## MAJOR RESTRUCTURE + BUILD-LANE PRICING (July 11, 2026)

Justin gave 18 notes to "perfect" the page (benchmarked against **tenex.co**). Built via a multi-agent swarm (3 creative-direction agents -> synthesis -> 4 adversarial QA agents: notes-compliance, honesty, design/CSS, conversion), screenshot-verified with headless Chrome, deployed. **The Hormozi flow in the paragraph above is GONE.**

**New section order:** Hero (decluttered) -> **Thesis** ("Go AI-native, or get out-operated", adapts tenex's AI-absent->AI-native + "win the next decade") -> **Offer + Guarantee together (3rd section)** -> **Bios (Ian, Justin, Alec NAMED)** -> **Proof (WHITE testimonial section)** -> What we build (50+ grouped) -> How it works (3 steps, "You describe it. We build it. You own it.") -> Apply -> FAQ -> Close. **Removed entirely:** the value-stack section, the 4-bonuses section, the two-column in-house-vs-monthly comparison (folded to one "$500k/yr to hire vs $5k/mo" line), the eyebrow "$1M-$50M" qualifier, the coupon-y $8k/Aug-15/"4 of 10 seats" scarcity (now just "founding cohort, limited seats at this rate", no number/date).

**PRICING CHANGED to build lanes:** "**Starts at $5,000/mo per active build lane**" (one active build at a time + unlimited queue). "Add a build lane at the same rate to ship more in parallel" (per-lane price defaulted to $5k = the natural read of "starts at $5,000"; **Justin can change this number**). Larger rollouts/enterprise = custom on the call. Keeps the door open up (explicit) and down (quietly, on the call). No more hard $8k ceiling.

**Guarantee redesigned** from a dashed "coupon" to an elegant **warm off-white embossed "110%" seal card** (double hairline ring, ink monochrome, three chips "Keep the build / Keep the code / Owe nothing", signed **"Ian, Justin & Alec"**), sitting in the dark offer section as a brightening beat. Copy: first build, not thrilled = pay nothing + keep the work; after that cancel any month (total risk = one month).

**Bios:** Ian, Justin, Alec are now **publicly named** (Justin OK'd it). Framing = "founder-grade minds inside your company, not worker bees" (note 18). **Justin's bio has his real, defensible track record** (founded/led growth for startups now doing $100M+/yr: one he started & sold, one he scaled from low-7-figures through a $50M+ raise). **Ian's and Alec's bios are still generic/role-based placeholders pending real per-person detail from Justin.** The old collective "$100M valuation" claim was corrected to "$100M+ annual REVENUE" (accurate) and lives in the Proof stat tiles + Justin's bio.

**Testimonials = big clean WHITE section** (`section.paper`, brightens the dark page per tenex). Uses **6 real named session testimonials** pulled from `/testimonials.js` DATA (Susan D'Elia, Peggy Tsai/JPMorgan, Farha Akhtar, Johan Jimenez/Amazon, Mark Yoldi, Jennie Kimmel/O'Reilly) with headshots from `/testimonials/headshots/`, each tagged "Live AI4NTP session" and framed "session reactions, not client case studies" (fixes note 7: the old disingenuous JPMorgan/Amazon/CMU/O'Reilly **logo row** implying they're clients is GONE, replaced by the real named person + honest label). Plus 3 track-record stat cards (Hundreds of agents / Zero to $100M+ revenue / We run on our own agents). **There are still NO paying agents clients** (founding cohort), so honesty framing is load-bearing.

**Also:** free maintenance now promised ("we maintain and support every build, free, while a member" pricebox bullet + FAQ); apply form is **email-only-required** now (company-size optional, revenue labels neutralized to Just starting->Enterprise but underlying value codes unchanged); removed a fabricated "roughly 90% of client work" FAQ stat; NO em dashes (verified 0). Design system unchanged (lime `#D4FF3A` accent, Fraunces/Instrument Serif/JetBrains Mono/Hanken, square geometry). One consistent `.eyebrow` class sitewide with a `.on-paper` color-only variant for white sections (note 8).

**Open follow-ups (not yet done):** enrich Ian & Alec bios with real detail (their BIO TEXT is still generic; photos are now added); verify/drop Mark Yoldi's company "Innovate Labs" (unverified, no URL in testimonials data, still live); optionally add an FAQ clarifying the 110% billing mechanic (charged-then-refunded vs not-charged-until-accepted).

**Same-day iteration (still July 11, after first deploy) per 7 Justin tweaks:** (1) hero H1 is now TWO parts, "Your on-demand AI department" (white) then a `<br>` and "building anything you can imagine." wrapped whole in `.hl` (yellow Instrument-Serif italic); (2) removed the hero "Built by Ian, Justin, and Alec..." trust line entirely; (3) the `From $5,000/mo. You own everything. First build 110% guaranteed. Cancel anytime.` reassurance line forced to ONE line (`.hero .microcta` white-space:nowrap, wraps again under 620px); (4) offer pricebox `.psub` changed to "**Try 100% risk free. Cancel anytime.**" (build-lane detail now lives ONLY in the FAQ, not the visible offer box); (5) removed the build-lane `.pfine` under the Apply button; (6) removed the `.themath` "$500k in-house vs $5k" comparison line; (7) **operator photos added** to the three bios: `agents/justin.jpg` / `agents/ian.jpg` / `agents/alec.jpg` (copied from `sessions/006/images/{justin-novak,ian-kilpatrick,alec-saluga}.jpg`, sips-resized to 400px ~30KB each), rendered as 72px circular `.biocard .bphoto`. Redeployed + verified live.

**Advisor-driven + Justin iteration (July 11-12 2026), CURRENT state:** Justin ran a trusted-advisor session (transcript) + several follow-ups. Changes shipped: (a) **Guarantee is now 100%, not 110%** (advisor: 110% reads greedy), reframed POSITIVE ("You are going to be thrilled with your first build. Guaranteed." + money-back as the safety net, not "if it doesn't thrill you"). (b) **Pricing = build lanes:** $5k/mo per active build lane, add lanes at same rate; but the visible offer `.psub` just says "**Try 100% risk free. Cancel anytime.**" (build-lane detail lives only in the FAQ). (c) All CTAs now "**Apply now**" (was "Apply for a seat/founding seat" — advisor: "founding seat" read like buying equity); apply section h2 "Apply to work with us." (d) **What we build** moved ABOVE bios (right after offer/guarantee) and converted to **collapsible `<details class="bcat">` dropdowns** (2-col grid, first open) to cut the wall of text. (e) **Thesis** condensed to ONE paragraph; headline is now "**Go AI-native while it is still an edge.**" (positive, was the harsh "or get out-operated"); close line "We are not ONLY here to help you win the next decade. We are here to help you win right now." (f) **Section order now:** Hero -> Shift -> **How it works** (moved up, right after Shift) -> Offer -> **Guarantee (now its OWN full-width WHITE `.paper` section**, was a card floating in the dark offer section) -> What we build -> Bios -> Proof -> Apply -> FAQ -> Close. (g) **EYEBROW BUG FIXED:** `.shift p` and `.close p` (big body-copy rules) were overriding the `.eyebrow` `<p>` inside them (higher specificity), making "The shift" and "Last thing" eyebrows huge; scoped both to `p:not(.eyebrow)` so all eyebrows now match "Agents as a Service" (12px lime mono). (h) Hero h1 forced to **2 lines** (`.hero .inner` max-width 1040px, h1 clamp cap 3.7rem) so "Your on-demand AI department" holds one line above the yellow-italic "building anything you can imagine."; hero reassurance line is one line ("First build 100% guaranteed"). (i) Bios intro trimmed to one short paragraph, collective paragraph removed; how-it-works "You describe it. We build it. You own it." forced to one line via `.shead.wide { max-width:none }`, and its "Your total effort..." steps-note removed. Structural section moves done via python scripts (scratchpad `move_build.py` / `move_how.py`).

**Advisor-driven revision (July 11, third deploy) from a recorded advisor session (Alec, marketing eye) Justin uploaded as a transcript:** (1) **flipped the thesis close** from "help you win the next decade" to "help you **win right now**" (advisor: AI is a short-term edge, sell winning TODAY not the decade), and changed the closing-section headline "The next decade is already being built" -> "**The winners are being decided right now.**"; (2) **110% -> 100% everywhere** (seal, offer bullet, hero reassurance, how-it-works, FAQ, JSON-LD) because "100% is 100%, anything more reads greedy" (NOTE: this reverses Justin's original note-10 "110%" preference, done with his OK); (3) **guarantee reframed POSITIVE** to kill the negative-first framing: vow is now "You are going to be thrilled with your first build. **Guaranteed.**" with the money-back as the safety net in the fine print (was "If your first build does not thrill you, you pay nothing"); (4) **offer names the team**: first pricebox bullet "A senior team" -> "**Ian, Justin, and Alec** building your agents and automations, the same operators you watch every week"; (5) **"Apply for a founding seat" was confusing** (read as buying equity) so ALL apply CTAs -> "**Apply now**" and the apply-section H2 -> "**Apply to work with us.**"; (6) **"What we build" moved UP** to immediately after the Offer+Guarantee (was after Proof) so it answers "is this for me?" early, AND converted from an always-open 4-col grid into **8 collapsible `<details class="bcat">` dropdowns** (first one open) to kill the wall of text; new `.bcat` CSS replaced `.bmenu`/`.bcol`; lead reworded to "If a person on your team does it on a screen, we can probably automate it. Open any that sound like you." The move was done via `scratchpad/move_build.py` (delete block + reinsert before `<!-- BIOS -->`). Section order is now Hero -> Thesis -> Offer+Guarantee -> What we build -> Bios -> Proof -> How -> Apply -> FAQ -> Close. Deployed + verified live. (Ignored transcript tangents; kept "founder-grade minds" which the advisor liked.)

**Apply flow (NEW, replaces the old book-a-call + Stripe stub):** all "Apply" CTAs (`href="#apply"`) scroll to an on-page application `<section id="apply">` (first name / email / company / annual-revenue radios / what-you-want-built). Submit POSTs to Supabase **`ai4ntp_signups`** (same anon-key INSERT-only pattern as `/build`, 201/409 = success), `source:'agents-apply'`, `interests:['agents-apply:<band>']`, `AI4Ref.attach` for referral credit; on success it reveals a "You're through, pick a time" success block with a `/calendar` button and a 1.4s redirect. Fires `agents_apply_success` (GA4). The old Stripe `CHECKOUT_URL` stub was **removed**. Reuses the existing Beehiiv `pg_net` trigger on `ai4ntp_signups` (no new SQL). See [[ai4ntp-build-lead-magnet]] for the source pattern.

**OG card regenerated (Jul 8 2026):** `agents/og-image.jpg` is now the standard light AI4NTP brand card (paper bg, `ai4ntp` wordmark, kicker "AGENTS AS A SERVICE", Fraunces headline "Your on-demand AI *department*." with "department" lime-highlighted, "From the operators behind AI4NTP", AI4NTP.COM). Config at `agents/og.json` (brand mode); rebuild with `node tools/og/render.mjs agents/og.json`. The old dark Anvil card is gone. Deploy: `vercel --prod --yes` (see [[ai4ntp-deploy-and-preview]]). See [[ai4ntp-partner-deck]], [[ai4ntp-internal-hosting]].
