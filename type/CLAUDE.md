# AI4NTP

Marketing site at https://ai4ntp.com. Vercel-deployed static HTML with `cleanUrls: true`.

## Recent work log

**July 9, 2026 - Zoom auto-registration (real-time + backfill):**
- **Session signups now auto-register onto the current Zoom webinar** (no more hand-typing each name/email into Zoom). Mirrors the `beehiiv-sync` architecture: a Supabase `pg_net` `after insert` trigger on `ai4ntp_signups` (`zoom_register_row()`, `supabase/ai4ntp_zoom.sql`, swallow-all exception guard so it can never block a signup) POSTs each new row to **`api/zoom-register.js`** (secret-verified via `x-webhook-secret` == `ZOOM_SYNC_SECRET`), which gets a **Zoom Server-to-Server OAuth** token (cached in module scope ~1h), derives a first name from the email local-part (the form is email-only), and calls the Zoom **add-webinar-registrant** API. Zoom then emails the person their unique join link + calendar invite automatically, exactly reproducing the manual add. Idempotent (an already-registered email is treated as success; a ledger row in `ai4ntp_zoom_registrations` lets backfill skip it).
- **Auto-targets the current webinar + pattern source gate (zero per-session config):** `getCurrentWebinar()` in `api/zoom-register.js` asks Zoom for the **soonest upcoming webinar** (cached ~5 min), so rotating sessions needs NO Vercel/code change (just schedule the new webinar in Zoom + point the register pages at `episode-00X`). It **requires the `webinar:read:list_webinars:admin` scope** on the S2S app (added + verified July 9, 2026 - ping returns `source:auto`); without it the list call 400s (code 4711) and it **falls back to the `ZOOM_WEBINAR_ID` env var** (which then becomes the per-session knob). The source gate is pattern-based (`SESSION_SOURCE_RE = /^episode-\d+-register/i`), so on `ai4ntp_signups` only session register-page signups get registered (NOT /build `build-page`, /agents), and it auto-follows rotation. **The homepage is also covered:** any insert to the legacy `signups` table (which is exclusively the homepage "Save your seat" funnel, source `homepage · <host>`) registers regardless of source (trigger `zoom_register_signups`, gate `body.table === 'signups'`). So real-time registration covers **/sessions/00X + /register + homepage**; it excludes /build, /agents, /partner. `GET /api/zoom-register?secret=<ZOOM_SYNC_SECRET>` pings the detected webinar (`source: auto` vs `env`) as a sanity check. (`ZOOM_SOURCE_PREFIX` env is now unused by the gate.)
- **Backfill (paced cron drip, whole list):** `api/zoom-drip.js` + a Vercel cron (`vercel.json` `"crons"`, `* * * * *`) trickle-registers EVERY unique email ever collected (across `ai4ntp_signups`, legacy `signups`, `ai4ntp_partner_apps`, and the `ai4ntp_pulse_00X` vote tables) onto the current webinar, at **~2/min** (a small randomized batch per cron tick, each 15-35s apart, within a 52s per-tick budget so runs never overlap and Zoom never sees a burst). Fully deduped + idempotent (lowercased email, unique `ai4ntp_zoom_registrations.email`, skips anyone already in the ledger), so each person is registered/emailed exactly once regardless of how many times/where they signed up. Runs server-side so it reuses the deployed env vars (no local secrets). `?status=1&secret=<ZOOM_SYNC_SECRET>` reports `totalUnique/registered/remaining` + per-table counts; `?n=K&secret=...` registers K immediately (bypass pacing, e.g. to finish the tail). Auth: Vercel cron `Authorization: Bearer <CRON_SECRET>`, or `?secret=<ZOOM_SYNC_SECRET>` for manual. **This is a ONE-TIME job: remove the cron entry from `vercel.json` and redeploy once `remaining` hits 0**, else it fires every minute forever (harmless no-op, but wastes cron quota). `ai4ntp_pulse_005` does not exist in the DB (skipped, fail-open per table). A local `tools/zoom-backfill.mjs` also exists (source-scoped, needs local creds) but the cron drip is the used path. NOTE the ledger is email-unique, so a person registered for 006 is NOT re-backfilled for a later session; only the real-time trigger handles new signups after rotation. **Backfill ran + completed July 9, 2026 (82 registered); the cron entry was then removed from `vercel.json`.** Terminal per-registrant failures (Zoom's **3/day per-email cap**, and **panelists**, which Zoom rejects with `code 3000` "invited as a panelist") are recorded as `skip:<reason>` ledger rows by `handleRegError()` so they are not retried every tick (this retry-waste was what stalled throughput mid-backfill); `?status=1` breaks out `skipped` + `skippedEmails`. Only skips were the two panelists (alec@workwithaero.com, ian@ianpk.com).
- **NOT doing capture-then-redirect** (that double-form is the funnel leak): `ZOOM_REGISTER_URL` stays `''` in `sessions/006/index.html`. Success copy on both 006 forms now names the Zoom join link ("We just emailed your Zoom join link").
- **New Vercel env vars (Production):** `ZOOM_ACCOUNT_ID`, `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, `ZOOM_WEBINAR_ID` (numeric, `87317768498` for 006), `ZOOM_SOURCE_PREFIX` (`episode-006`), `ZOOM_SYNC_SECRET`, and `CRON_SECRET` (secures the cron drip). **LIVE + verified end-to-end July 9, 2026** (real-time trigger tested via a live signup; backfill trickling at ~2/min). Setup that was done: Zoom S2S OAuth app (scopes `webinar:write:registrant:admin` + `webinar:read:registrant:admin` + `webinar:read:webinar:admin`, activated); 006 webinar set to Registration Required + Approve Automatically + confirmation email ON; env vars set (gotcha: `vercel env pull` shows Sensitive values as empty, so verify via the live function, not the pulled file); `vercel --prod --yes`; ran `supabase/ai4ntp_zoom.sql` in the SQL editor with the real `ZOOM_SYNC_SECRET`. The numeric webinar id is on the Zoom webinar's Details page ("Webinar ID"); `tools/zoom-backfill.mjs --discover <WN_token>` can also find it.

**July 5, 2026 - Referral / affiliate tracker (custom, first-party):**
- **Built a custom referral program** so registrants can invite friends and unlock rewards. Every signup (or the `/refer` page) issues a personal short link **`ai4ntp.com/r/<code>`**; a private, token-gated portal at **`ai4ntp.com/portal?t=<token>`** gamifies progress (link clicks, who signed up, milestones). Rewards are **5 mysterious, prestige-named tiers** (contents stay hidden until unlocked, to keep it aspirational): **3 = Insider**, **5 = VIP**, **10 = Inner Circle**, **20 = Vanguard**, **50 = Legend**. Only Insider (3) has a concrete reward wired so far: the free 30-min `/build` session, revealed + claimable in the portal on unlock. 5/10/20/50 reveal a "we'll be in touch" note until their rewards are defined. Tier list is defined in three places kept in sync: `TIERS` in `api/portal-stats.js`, the rail in `portal/index.html`, and the ladder on `/refer` (plus `THRESHOLDS` in `tools/reconcile-referrals.mjs`).
- **Attribution beats the Zoom/Eventbrite leak** by design: the shared link is always an AI4NTP URL (never a raw Zoom/Eventbrite link), and the friend's email is captured on our page before any external redirect. `/r/<code>` (`api/r.js`) logs a click, sets a first-party `a4ref` cookie (30d, first-touch), and 302s to the **current session** page carrying `?ref=<code>` (EDIT POINT: `CURRENT_SESSION` const in `api/r.js`, currently `/sessions/006`).
- **Credit basis = signup, live-counted; reward = attendance-gated.** The portal counter moves the instant a friend signs up (real-time), but the reward CLAIM activates only when they attend, reconciled post-event (see below). A friend is credited to exactly ONE referrer forever (global unique `referred_email` + first-touch cookie).
- **Data model** `supabase/ai4ntp_referrals.sql` (run it in the SQL editor): 3 new tables, all **service-role only, zero anon access** (RLS enabled + no policies + revoked grants): `ai4ntp_affiliates` (email-unique idempotency, `code` + secret `portal_token`), `ai4ntp_referral_clicks` (ip_hash not raw IP, `is_bot` flag), `ai4ntp_referrals` (per-referral `status` pending/attended/rewarded/disqualified, `referred_email` globally unique). Adds a nullable `ref_code` column to BOTH `ai4ntp_signups` and legacy `signups`, and a `security definer` trigger `referral_capture_row()` (mirrors `beehiiv_sync_row()`, uses `to_jsonb(NEW)` so it is column-agnostic across both signup tables) that mints a referral when a signup carries a valid `ref_code`, blocking self-referral and deduping. Plus a `beehiiv_sync_affiliates` trigger.
- **Endpoints** (all mirror `api/state-005.js`'s `supabaseFetch` service-role helper): `api/r.js` (redirect), `api/refer-link.js` (idempotent affiliate create/retrieve, returns `{code, portal_token, share_link, portal_link}`), `api/portal-stats.js` (stats by token, `noindex`). New `vercel.json` rewrite `/r/:code -> /api/r?code=:code`. `ai4ntp_affiliates: 'Referral Program'` added to `SOURCE_BY_TABLE` in `api/beehiiv-sync.js`.
- **Client wiring:** shared `/ref.js` (capture: `AI4Ref.attach(payload)` sets `ref_code`; `AI4Ref.myCode()`) + `/refer-block.js` (auto-issue renderer, `AI4Refer.issue(email, name, mountEl)`). **All 4 signup forms now capture `ref_code`** (build, register, sessions/006, homepage both forms). **Auto-issue block shows on the capture-only surfaces** (`/sessions/006` success + homepage closing CTA); `/build` and `/register` capture attribution but keep their calendar/Zoom redirects untouched (deliberate: do not hijack the handoff). `/sessions/006`'s `#invite` widget personalizes its share link to `/r/<mycode>` when known. New pages: `portal/index.html` (dark, `agents/`-style, polls stats), `refer/index.html` (light chrome, nav/footer).
- **Attendance reconciliation is a standing post-webinar step** (Justin's requirement): `tools/reconcile-referrals.mjs` reads the Zoom attendee CSV (`Attended`/`Email`/`Time in Session` columns), flips matching `pending` referrals to `attended`, and reports milestone crossings. Wired into `sessions/_template/post-webinar-playbook.md` Phase 3 next to the Beehiiv/CRM sync. Local script (not an endpoint) because the CSV is PII under the `.vercelignore`'d `revops/`.
- **No new env vars** (reuses `SUPABASE_SERVICE_ROLE_KEY` + `BEEHIIV_*`). **Deferred:** concrete rewards for 5/10/20/50 tiers, transactional email delivery of links (no transport in-app yet; on-screen + portal link only), automated attendance reconciler, click-dedup index.
- **July 5-6 update - LIVE, reframed to mysterious tiers, + admin dashboard:** the whole system is deployed and verified in production (real auto-issued affiliates already showing up). `/refer` is a **5-step sequential how-it-works page** (each step an H2 with a number badge + a visual): 1 Get your link (the form), 2 Share your link (channel chips that activate into real share buttons on submit), 3 Track your referrals (dark dashboard-preview mockup + an open-your-dashboard link), 4 Unlock exclusive rewards (the horizontal Insider->Legend named-tier ladder, rewards hidden until unlocked), 5 Climb the leaderboard (an **illustrative** leaderboard mockup, "A live monthly leaderboard is coming soon", no backend yet). "Refer & earn" lives in `footer.js` (Get involved) ONLY, not in `nav.js` (removed at Justin's request). The auto-issue block (`refer-block.js`) invite copy does not name the tiers. New **admin dashboard at `/internal/refer`** (`api/internal-refer.js`, SAME HTTP Basic Auth as `/internal`, realm "AI4NTP Internal", added to `vercel.json` rewrites): server-renders a live table of every affiliate with signups / attended / pending / human clicks / conversion % / tier / source / joined, plus summary cards + top referrer. It reads Supabase with the service-role key at request time (no client fetch).

**July 2, 2026 - Anvil: public Agents-as-a-Service sales page at `/agents`:**
- **Shipped the first PUBLIC sales page for the Agents as a Service offer**, to start advertising it. Live at **`ai4ntp.com/agents`**. New product brand **Anvil** ("Your AI department, forged"), presented as "by AI4NTP" / from the operators behind AI4NTP (Ian and Alec are not publicly named; they have not formally signed). Legal entity in footer: T Minus Studios.
- **Standalone dark page** in the deck/dashboard aesthetic (ink `#0E1116`, amber `#F5B133` = brand/forge, teal `#4FD6C4` = CTAs/money, red = urgency), system-ui + SF Mono, NOT the light marketing chrome. Single self-contained file `agents/index.html` (no `nav.js`/`footer.js`, its own slim sticky bar + footer). No `vercel.json` change needed. Co-located dark OG card at `agents/og-image.jpg` (custom-rendered, not the light AI4NTP brand card).
- **Structured as an Alex Hormozi Grand Slam Offer** with classic direct-response copy: hero price-anchor headline ($500k department vs $5k/mo), the "build the wedge now" market-shift section, the hire-the-department-vs-hire-Anvil comparison, value stack with strikethrough ($8k -> $5k), 4 stacked bonuses incl. a "bonus of the bonus" (founding price lock), the 110% money-back guarantee, why-us (50 years + AI jet fuel), what-we-build grid, how-it-works, Designjoy-style pricing box, 13-item objection-crushing FAQ (mirrored to `FAQPage` JSON-LD), and a close. `Service`+`Offer`+`FAQPage`+`Organization` JSON-LD. No em dashes.
- **Offer terms (EDIT POINTS live in the copy):** $5,000/mo founding price, doubling to $8,000 on **Aug 15, 2026**; **10 founding seats, 4 left**; unlimited builds, ~1/week, one active in queue; tokens/infra covered; you own the IP; cancel anytime.
- **CTAs:** primary "Book your fit call" -> `/calendar` (existing 15-min Google embed; a dedicated longer fit-call booking would be an easy upgrade). Secondary "Ready now? Start today" is a **Stripe stub**: set `CHECKOUT_URL` in the page's bottom `<script>` to a $5k/mo Stripe Payment Link to enable it; until then it falls back to `/calendar` (never broken). GA4 fires via `data-cta` attributes (`agents_book_call_*`, `agents_checkout_start`, `agents_faq_open`) + a `book_call_click` with operator. **No Stripe exists in the repo yet** (greenfield if you ever want real self-serve checkout via `api/`).
- **Not built (deliberately):** live Stripe integration (stub only), lead-capture table (optional phase 2: reuse the Supabase anon-key POST pattern into a new `ai4ntp_agents_leads` table + `SOURCE_BY_TABLE` + trigger for Beehiiv). Brand name is a single find-replaceable token; verify `anvil.com`/TM before hard-committing.

**July 2, 2026 - Internal partner package (deck + two interactive models), password-gated at `/internal`:**
- **A private, password-gated partner pitch package now lives on the site**, for aligning Justin, Ian, and Alec. Three docs served by one serverless function `api/internal.js` doing **server-side HTTP Basic Auth** (real gate, not client-side theater): the HTML is embedded as **base64** inside the function so it never exists as a public static file, and is only sent after the password matches. `vercel.json` `rewrites` map `/internal` (+ `/internal/brief`), `/internal/model`, `/internal/growth`, and `/internal/growth-notes` to it (`?doc=brief|model|growth|growth-notes`). Responses carry `X-Robots-Tag: noindex`. **Password lives in `api/internal.js` (`FALLBACK_PW`) or env var `INTERNAL_PW`, not in any tracked doc.**
  - **Partner deck** at `/internal` (14 slides + appendix): vision, opportunity, traction, testimonials, base-vs-bull, roadmap, launchpad flywheel, monetization, Agents as a Service, revenue math, roles/equity, ask. Source `scratchpad/ai4ntp-partner-brief.html`.
  - **Revenue model** at `/internal/model`: interactive Agents-as-a-Service unit economics (funnel to deliverable MRR, capacity bottleneck). Source `scratchpad/agents-revenue-model.html`, math verified by `scratchpad/model_test.js`.
  - **Growth engine** at `/internal/growth`: interactive 52-week go-to-market simulation with a viral K-factor loop, five scenarios from autopilot (~11k) to full send (~498k). Source `scratchpad/ai4ntp-growth-model.html`; shared math core `scratchpad/growth-core.js` validated by `scratchpad/growth-test.js` (32 assertions). Its written notes render from `revops/growth-engine.md` and are hosted at `/internal/growth-notes`.
- **Written companions in `revops/` (PRIVATE, never deploys):** `partner-deck.md`, `revenue-model.md`, `growth-engine.md`. Read those for the full context; `revops/README.md` indexes them. The scratchpad HTML sources are ephemeral, so the durable record is these docs + the deployed `api/internal.js` + the claude.ai artifacts.
- **To rebuild/redeploy the internal docs:** edit the scratchpad HTML (or `revops/growth-engine.md` for the notes page), run `node scratchpad/build-internal.js` (regenerates `api/internal.js` from the sources; converts the growth-engine markdown to a styled reading page), then `vercel --prod --yes`.
- **Security fix:** root `.md` files were being served publicly (`CLAUDE.md`, `growth-plan-Y1.md` returned 200 on the live site). Added them to `.vercelignore` so they no longer deploy. `brand-kit.md` was left public pending a call on whether it is intentionally shareable. `revops/` was already correctly excluded.

**June 30, 2026 - Episode 005 brief-picker (live vote app) built:**
- **Recreated the 002 brief-picker for Episode 005.** Audience submits "What should we build?" -> Claude synthesizes 5 candidate companies -> audience votes -> host locks the winner, which becomes the single brief all three builders (Ian/Justin/Alec) race to build. Same three-table + two-route + two-page architecture as 002.
- New files: `supabase/ai4ntp_pulse_005.sql` (tables `ai4ntp_pulse_005`, `_options`, `_state` + RLS + realtime + beehiiv_sync_pulse005 trigger), `api/synthesize-005.js` (Claude Sonnet 4.6; prompt tuned to **real, buildable** companies, not 002's funny/fake framing, matching 005's "three real companies" positioning), `api/state-005.js` (phase/lock/reset), `sessions/005/assess/index.html` (audience), `sessions/005/assess/live/index.html` (host dashboard). Added `ai4ntp_pulse_005: 'Live Session 005'` to `SOURCE_BY_TABLE` in `api/beehiiv-sync.js`.
- **Reuses existing env vars** (`SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`) and the same Supabase project. **No auth** (host page is private by URL obscurity + noindex), same as 002.
- **TO GO LIVE: (1)** run `supabase/ai4ntp_pulse_005.sql` in the Supabase SQL editor (tables do not exist yet), **(2)** `vercel --prod --yes`. URLs: audience `/sessions/005/assess`, host `/sessions/005/assess/live`. Do not share the host URL (anyone with it can synthesize/lock/reset).

**June 24, 2026 - Episode 005 time update:**
- **Episode 005 moved to 1:00 PM Eastern.** Updated `nav.js` countdown (`EVENT_START = 2026-06-30T13:00:00-04:00`, `EVENT_END = 2026-06-30T14:00:00-04:00`), `/sessions/005`, `/register`, homepage card/CTA, `/sessions` hub, `sessions/005/og.json`, `sitemap.xml`, and `llms.txt`.

**June 23, 2026 - Episode 005 launch page and countdown:**
- **Episode 005 is dated and live in the site funnel.** Tuesday June 30, 2026, 1:00 PM Eastern. Public framing: **"Three builders, one live AI build-off"**. Brett moderates while Ian, Justin, and Alec each build the same audience-voted app/company/project in parallel, with the audience voting on the strongest build. Created `/sessions/005/` with dark "build arena" design, Event + FAQ schema, builder lanes, format, scoring, run-of-show, FAQ, and `/register` CTAs. Co-located `sessions/005/images/*`, `og.json`, and generated `og-image.jpg`.
- **Current top-of-site countdown now points to Episode 005.** `nav.js` uses `EVENT_START = 2026-06-30T13:00:00-04:00`, `EVENT_END = 2026-06-30T14:00:00-04:00`, label `Episode 005 · live June 30 · begins in`, and CTA `/register`.
- **Homepage, sessions hub, register page, footer, sitemap, and llms.txt now surface Episode 005.** Homepage `#sessions` leads with EP005; `/sessions` lists 005 first and updates ItemList schema; `/register` now explicitly says "Count me in for Episode 005" and captures leads only; `footer.js` links Episode 005 live and Episode 004 replay; `sitemap.xml` and `llms.txt` include `/sessions/005`.

**June 5–7, 2026 — Episode 003 prep, AI tools directory, revops:**
- **Episode 003 is dated and open for registration.** Wednesday June 10, 2026, 6 PM EST. Headline **"Build your own AI Clone Agent in under an hour"** (**renamed June 9, 2026**, in two steps: "AI **employee**" → "AI clone" → "**AI Clone Agent**", across `/sessions/003`, the homepage card, `sessions/index`, all social meta + JSON-LD, `llms.txt`, and all six OG image configs/renders — softening the "replacing humans" optics after audience pushback; originally "Set up your own OpenClaw, live"). Note: the OpenClaw tool tagline ("run like a digital employee") and the internal `sessions/003/*.md` planning docs + cold-email copy still say "employee". **Ian Kilpatrick builds, Justin moderates** (Alec is out of town). Zoom registration is live; on-site forms **capture → redirect to Zoom** (`ZOOM_REGISTER_URL`, **250-seat cap**). Eventbrite is live (banner image in `sessions/003/`). Finalized marketing copy saved at `sessions/003/marketing-copy.md`.
- **OG/social image engine rebuilt** (`tools/og/render.mjs`, headless Chrome + real Fraunces; replaced the Pillow scripts). All OG cards regenerated (home, 001, 002, 003). Full 003 promo set generated and co-located in `sessions/003/`: `og-eventbrite.jpg` (1880x940, square-safe), `og-ig-square/portrait`, `og-linkedin`, `og-zoom-banner`, `og-zoom-logo`.
- **AI tools directory shipped** at `/ai-tools` (see its bullet under Project structure). World-class SEO/LLM build, Google-Sheet editing pipeline.
- **Site polish:** calendar booking switched to **15 minutes**; footer reworked (Connect→Socials + new Connect column, even spacing); homepage section order flipped so **Sessions sits above Testimonials**; hub-style testimonials + **Peggy Tsai (JPMorgan / Carnegie Mellon)** testimonial added.
- **`revops/` created** (PRIVATE, never deploys). Full monetization strategy + **code-verified** analysis of the 001/002 attendee data (B2B ICP ≈ **72–88 of 315 unique people**; the 001 cold-email engine produced ~80% of ICP) + a high-value-contacts outreach tracker. **Read `revops/README.md` first when revisiting strategy/monetization/leads.** Headline conclusion: cheap cold top-of-funnel is proven; **revenue is the unbuilt bottleneck** — the next move is a paid offer + conversion step + 3 pilots, not more audience.

## Design system

**Green `//` section eyebrow (June 9, 2026):** every homepage `.section-label` (Sessions, Why
AI4NTP, FAQ) is prefixed with a green `//` via `.section-label::before { content: '//'; color:
var(--signal-deep) }`, matching the hero's `.hero-eyebrow::before`. This is the intended sitewide
motif but is **only on the homepage so far** — other pages (`sessions/00X` `.block-label`,
`register`/`about`/`partner` inline `.section-label`, generated `ai-tools/**`) still show the old
dash and should adopt the `//` (ideally as a shared `base.css` rule in the pending design-system refactor).

## Voice and tone

**Never use em dashes (—).** Use commas, periods, parentheses, or restructure the sentence. This rule applies to all user-facing copy on the site.

The AI4NTP voice is direct, declarative, slightly understated. Specific over generic. No hype, no hedging. Built for non-technical operators learning AI.

## Project structure

- `index.html`: homepage, a registration funnel. Header: **as of June 9, 2026 the homepage uses the shared global `nav.js` header** (the old inline `<header class="site-header">` and its inline countdown/mobile-menu script were deleted; the form script was kept). So the countdown bar + nav (logo, `Save your seat` → `/register`) are now the same component as every other page. See the `nav.js` entry below. Section order (**reordered June 9, 2026; reordered again June 15, 2026** → hero → Testimonials → Sessions → Why AI4NTP → FAQ → closing CTA): hero (redesigned June 8, 2026, **centered**: mono eyebrow `// AI FOR NON-TECHY PEOPLE` (`.hero-eyebrow`, the `//` is decorative via `::before`) above the `ai4ntp` wordmark, then a single Fraunces serif headline `.hero-headline` "Real operators show you exactly how they use and build with AI so you can become the most AI proficient person in the room." (the old `Become the most AI-capable person in the room.` emphasis line + descriptive paragraph were removed), inline email capture at `#register` (the `.signup-note` `Free · Live on Zoom · Replay included` microline was removed June 9, 2026), then a `.hero-proof` block: overlapping avatar stack (4 testimonial headshots from `/testimonials/headshots/`) + `Join 450+ operators from organizations like` flowing into a mono company row `JPMorgan · Amazon · Carnegie Mellon · O'Reilly`. The 450+ = real registrations across 001–003; the four companies are real attendee affiliations) → **Testimonials** (injected via `/testimonials.js`; the dark "You are in good company." 1-up carousel) → **Sessions** (as of June 24, 2026 leads with Episode 005: `Three builders. One live AI build-off.`, `Live June 30`, four headshots, `1 PM ET`; then Episode 004/003/002/001 replays) → **Why AI4NTP** dark band (**extracted to the global `why.js` component June 9, 2026**) → **FAQ** (`#faqs`) → closing full-bleed CTA (`Your AI edge starts here.`). Both email forms (hero + closing) **capture to Supabase only** and show "You're in. We'll email you the Episode 005 details." No Zoom redirect until the Episode 005 Zoom link exists. Footer is injected via `/footer.js`. GA4 + Vercel Analytics installed.
- `sessions/<id>/index.html`: episode landing or recap pages.
  - `001` is the post-event recap: replay, transcript, FAQs, tools, pulse data, booking links to all three operators.
  - `002` is now the **published post-event recap** (shipped June 2, 2026; was the build-in-public live). The audience voted to build **GotoBuild**, an AI workout planner (logo: a yellow banana barbell); the live site is at `https://www.gotobuild.pro` and Ian's brand kit at `https://ianpk.com/gotobuild`. The recap page sections: replay (YouTube `AdylF0tNIbY`) → 5-min catch-up → book a 30-min (Ian/Alec/Justin) → tools/stack → "What the room wanted us to build" (static snapshot of the brief-picker vote, pulled once from Supabase: GotoBuild 10, Yeskia 8, KartPass 4, Watchdog 3, NOBALDIES 0) → "What we built" → bios (incl. Brett, co-moderator) → FAQ → searchable transcript → Episode 003 CTA. Source/working files in the folder: `youtube.md` (upload metadata), `transcript.md`, `faqs.md`, `tools.md`, `catchup.md`, `post-event-emails.md`, `thumbnail.jpg` + `generate-thumbnail.py` (1280x720 YouTube thumb), `og-image.jpg` regenerated with `status="REPLAY"`. The brief-picker pages (`/sessions/002/assess`, `/assess/live`) and `api/` routes are now dormant.
- `sessions/_template/`: canonical reusable assets.
  - `post-webinar-playbook.md`: the master system for every session (gather → process → publish, includes analytics wiring, mailer rate-limit lesson). For OG images it now points at the headless-Chrome renderer below (the old Pillow `generate-og-image.py` scripts were retired June 4, 2026).
- `tools/og/`: **OG/social card renderer** (added June 4, 2026, replaces the Pillow scripts). `render.mjs` builds an HTML card from a JSON config and screenshots it with **headless Google Chrome** (`puppeteer-core`, pointed at the installed `/Applications/Google Chrome`), using the **real Fraunces variable font** + actual brand CSS, at `deviceScaleFactor: 2` → 2400x1260 JPEG. Auto-fits the headline so it never overflows/wraps. Each card has a co-located `og.json`: `/og.json` (homepage brand card), `/sessions/00X/og.json` (episode cards). Run `node tools/og/render.mjs --all` to rebuild every card, or pass one config path. Two modes auto-detected: **episode** (`session_id`+`status` LIVE/REPLAY+`operators`) and **brand** (`kicker`+`subtitle`+`footer_note`). All four current cards (home, 001, 002, 003) were regenerated with this engine. The `tools/` dir (incl. `node_modules`) is in `.vercelignore`. Full docs in `tools/og/README.md`.
- `register/index.html`: standalone registration form (name, email, interests, goal). **As of July 6, 2026 it is the Session 006 register flow, CAPTURE-ONLY** (no Zoom link for 006 yet): hero pill `Session 006 · July 12 · 2 PM Eastern`, title `Save your seat for Session 006`, hidden source `episode-006-register-page`, success copy points back to `/sessions/006`. `ZOOM_REGISTER_URL = ''` so it captures only; the redirect is now conditional (`if (ZOOM_REGISTER_URL) …`) and dormant. When a Session 006 Zoom link exists, paste it into `ZOOM_REGISTER_URL` (top of the page script) to restore capture-then-redirect. Captures to Supabase `ai4ntp_signups` (+ `ref_code` for the referral tracker); below the form sit the shared **Testimonials** (`/testimonials.js`) and **Why AI4NTP** (`/why.js`) components. Footer via `/footer.js`.
- `calendar/index.html`: a Google Calendar appointment-schedule embed for booking a 1:1. Switched to **15-minute** meetings (June 5, 2026; was 30) — hero eyebrow reads "15 minutes" and the meta descriptions say "15-minute working session." The duration itself is set in Google Calendar; the page just embeds it. Linked from the footer ("15 Min Meeting") and from every `/ai-tools` page CTA.
- `testimonials/`: assets for the testimonials component. `testimonials/headshots/<slug>.jpg` are the headshots rendered in the carousel (`susan-delia`, `peggy-tsai`, `mark-yoldi`, `farha-akhtar`, `johan-jimenez`, `jennie-kimmel`, `sydney-kida-timmers`; square works well, a missing one falls back to the person's initials). Current order in the `DATA` array (7 testimonials): Susan D'Elia, **Peggy Tsai** (JPMorgan Chase / Carnegie Mellon, added June 7, 2026, 2nd slot), Mark Yoldi, Farha Akhtar, Johan Jimenez, Jennie Kimmel, Sydney Kida Timmers. The top-level `*.png` files are the **original LinkedIn-message screenshots** the quotes were transcribed from, kept for reference only (no longer rendered on the site). Not in `.vercelignore`, so they deploy.
- `testimonials.js`: **global testimonials carousel** component (see Testimonials). Injected via `<script src="/testimonials.js"></script>` on the homepage and `/register`.
- `privacy/index.html`, `terms/index.html`: Privacy Policy and Terms of Use pages (`/privacy`, `/terms`), on-brand, using `nav.js` + `footer.js`. Linked from the footer bottom bar. Baseline copy reflecting the real stack; the physical mailing address lives in the Beehiiv email footer, and Terms governing-law is generic ("the state in which T Minus Studios is established").
- `mailer/`: Apps Script + Google Sheets mailer for post-event email sends. Bridges Gmail draft + Google Sheet recipient list. Rate-limited to 10s/send by default to avoid Google Workspace burst throttling. README in the folder.
- `email-signature.html`: branded Gmail signature, generator + install instructions.
- `nav.js`: **global site header** (countdown bar + nav: logo, `Save your seat` → `/register`). Self-contained and namespaced (`.ai4nav*`), injected by `<script src="/nav.js"></script>` right after `<body>`. **As of June 9, 2026 the homepage (`index.html`) uses this too** (its inline header was deleted), so `nav.js` is now the **single header component** across `index.html`, session pages, `sessions/index`, `register`, `partner`, `calendar`, `about`, and all generated `ai-tools/**`. NOT on the live-tool pages (`/assess`, `/assess/live`, `/slides`). On mobile (`<820px`) the links collapse into the burger panel. Counts down to **Episode 005, June 30, 2026 1 PM Eastern** (`EVENT_START` = `2026-06-30T13:00:00-04:00`, `EVENT_END` 14:00); when it wraps it flips to "Episode 005 has wrapped" → `/sessions/005`. **Single source of truth: edit the header here only**.
- `why.js`: **global "Why AI4NTP" section** (added June 9, 2026). Full-bleed dark band: `// WHY AI4NTP` eyebrow → `For everyone trying to keep up with AI.` → three numbered cells (01 `Learn from real operators, live.` / 02 `Something for every level.` / 03 `Join the movement.`). Self-contained, namespaced (`.ai4why*`), injected by `<script src="/why.js"></script>` **at the script's position** (mirrors `footer.js`'s `currentScript` mount). Created to kill drift: the homepage and `/register` had diverged (`/register` showed stale copy), so **both now render this one component — edit the section here only.** Used on `index.html` + `register/`.
- `footer.js`: **global site footer** (dark multi-column band: brand + Save-your-seat CTA · Sessions · Get involved · Socials · Connect · bottom bar; 5-column grid). Self-contained and namespaced (`.ai4ft*`, with explicit resets so old per-page bare-`footer {}` CSS can't leak in), injected by `<script src="/footer.js"></script>` where the footer should sit. Used on **all** pages (homepage, register, partner, calendar, sessions/001, sessions/002), so the footer is edited in one place. Columns: **Sessions** (Episode 002/001 replays, Reserve your seat) · **Get involved** (Apply to be a guest, Sponsor an episode `/partner#apply`, Newsletter `https://ai4ntp.beehiiv.com`) · **Socials** (YouTube, X, Instagram) · **Connect** (15 Min Meeting `/calendar`, Email `mailto:justin@ai4ntp.com`); bottom bar has **Privacy** (`/privacy`) and **Terms** (`/terms`). (June 4, 2026: split the old "Connect" into **Socials** + **Connect**, moved Email under Connect, replaced "Book a 1:1" with "15 Min Meeting" pointing at the same `/calendar`.) Replaced the old per-page footers that had broken `#` social links and a stale `hello@ai4ntp.com`. Not yet added: LinkedIn, Community.
- `vercel.json`: Vercel config (uses `cleanUrls`, so image paths in sub-routes must be absolute, e.g. `/sessions/001/images/...`).
- `ai-tools/`: **programmatic-SEO AI tools directory** (added June 5, 2026). Public hub at `/ai-tools` + one page per tool at `/ai-tools/<slug>`. **Generated, do not hand-edit the HTML** — it's built from `tools/directory/tools.json` by `tools/directory/generate.mjs` (pure Node, no deps; reuses the head boilerplate + `nav.js`/`footer.js`). White-hat: each page leads with first-party usage (which episode/operator/used-for, from 001/002), plus pricing, **curated** alternatives (genuine substitutes only, from the data — never auto-category-dumped) and an honest "used alongside" row (same-episode tools), and an auto-derived (de-duplicated) FAQ. Each tool page splits **"What is X?"** (OBJECTIVE, pulled from the tool's own meta description via `tools/directory/fetch-descriptions.mjs`; falls back to the factual tagline) from **"How and why we use X at AI4NTP"** (our first-party take, the `ai4ntp_take` field) — the objective description lives in the `what_is` field/Sheet column. Also built for LLM + SEO: an answer-first summary line, a key-facts table, a conversion **CTA card** at the end (Register `/register` · Book 1:1 `/calendar` · Email `mailto:justin@ai4ntp.com` — doubles as a registrant funnel, fires `tool_register_click`/`tool_book_click`/`tool_email_click`), and a `robots` rich-snippet directive. Structured data per tool page: `SoftwareApplication` + `FAQPage` + `WebPage` (dateModified) + `BreadcrumbList` (+ sitewide `Organization`); hub adds `CollectionPage` + `ItemList` + an "About this directory" `FAQPage` + category quick-nav. The generator also writes **`/sitemap.xml`**, **`/robots.txt`**, and **`/llms.txt`** (LLM/answer-engine index) at the repo root. **Editing source of truth (June 5, 2026): a Google Sheet** (Tools tab + Episodes tab), synced via `tools/directory/sync.mjs` (fetches each tab published-as-CSV per `sheet.config.json`, rebuilds `tools.json`, regenerates). `tools.json` stays committed as the offline-buildable cache. `seed-tools.csv`/`seed-episodes.csv` seed the Sheet; `export-seed-csv.mjs` re-creates them from JSON. To update: edit the Sheet, run `node tools/directory/sync.mjs`, deploy. (Direct `tools.json` editing + `node tools/directory/generate.mjs` still works.) **Never fabricate `g2` ratings** (kept `null` until sourced; chip only renders when populated). Source + generator live under the `.vercelignore`'d `tools/`, so only the generated `/ai-tools/**` ships. Footer (`footer.js`) links to `/ai-tools` sitewide. Full docs: `tools/directory/README.md`. Seeded with 25 tools from Sessions 001 + 002.
- `blog/`: **programmatic blog / "field notes"** (added July 15, 2026). Public hub at `/blog` + one page per post at `/blog/<slug>`. **Generated, do not hand-edit the HTML** — built from `content/posts.json` by `tools/blog/blog.mjs`, which is imported by `tools/directory/generate.mjs` (**the single build entrypoint** for tools + blog + sitemap + robots + llms.txt; a standalone blog build would fight the generator over those three files and lose). Shared chrome (`head()`, `CSS`, `EP`, `PEOPLE`, `personLd()`, `crumb()`, `write()`) was extracted to **`tools/lib/site.mjs`** and is used by both generators, so the blog is not a design fork; blog-only rules ship as `BLOG_CSS` via the new `head({extraCss})` param. The refactor was verified byte-identical against a snapshot of all 52 tool pages.
  - **The bright line: one tool = `/ai-tools/<slug>`, many tools or a job-to-be-done = `/blog/<slug>`.** This is what stops the blog cannibalizing the directory. A single-tool query (pricing/review/alternatives/what-is/vs) always belongs to the tool page.
  - **The moat is evidence, and it is machine-enforced:** every numeric claim carries a `source.citations` entry (timestamp + speaker + actual quote) traced to `sessions/<id>/transcript.md`. This is why the posts can outrank higher-authority sites (LLMs cite sources of facts, not commentary about public facts) and it is the scaled-content-abuse compliance story. **`tools/blog/verify-citations.mjs` runs inside the build and REFUSES TO BUILD** on a timestamp that does not exist, a wrong speaker, or a non-verbatim quote (elision must be marked `...`; an unmarked splice is a fabricated quote). It parses both transcript formats. The generator also hard-fails on unknown tool slug / session / author / post ref.
  - **RULE: `sessions/<id>/transcript.md` is the ONLY valid timestamp source.** `catchup.md` and `faqs.md` are clocked to the **trimmed replay** and run minutes ahead, non-constantly (006: catchup `[0:05]` = transcript `[2:16]`; catchup `[42:25]` = transcript `[49:19]`). `catchup.md`'s "Best moments" is a pre-curated list of exactly the evidence you want, and harvesting it produces citations that are all wrong and look perfect. The verifier detects this specific failure and points at the real timestamp.
  - `tools/blog/cannibalize.mjs "<keyword>"` screens a candidate against posts + tools + sessions. It is advisory on the tools surface (false-positives on job-shaped queries containing a tool name, false-negatives on single-tool queries with no modifier), so the bright line still needs a human.
  - 4 archetypes: `listicle` (ItemList, grouped by job, never ranked), `howto` (HowTo + HowToStep + HowToTool), `explainer` (DefinedTerm), `case-study`. Every post emits `BlogPosting` + `WebPage` + `BreadcrumbList` (+ `FAQPage`), and **`BlogPosting.mentions` points at the `/ai-tools/<slug>#software` @id nodes the tool pages already emit**, so the directory and blog resolve as ONE graph. Note FAQ/HowTo rich results are deprecated (Google restricted FAQ to gov/health Aug 2023, dropped HowTo); the markup is for LLM extraction, not SERP features.
  - Seeded with the two `content/drafts/*.md` posts that had been blocked since June on the `/blog` vs `/field-notes` URL call (resolved: **`/blog`** for machines, branded "Field notes" in the UI). The evidence audit during migration caught an unsourced "~5 cents per social post", a paraphrase presented as a verbatim pull quote, and a missing Pulsar credit; all three were fixed, none shipped.
  - OG cards are automatic: the generator writes `blog/<slug>/og.json` from the record's `og` field and `node tools/og/render.mjs --all` discovers it. `footer.js` links `/blog` ("Field notes"). Full docs: **`tools/blog/README.md`**.
  - **`.claude/skills/session-to-blog/`** is the skill that mines a transcript into posts: two-phase (proposes a ranked slate and STOPS for approval, then drafts + generates), with `references/archetypes.md` + `references/geo-rules.md` + `templates/slate.md`. Transcripts exist for **002, 004, 005, 006 only**, and their format is inconsistent (006: `**[5:03] Ian:**`, 002: `**Ian · 08:29** —`).
- `revops/`: **PRIVATE internal business context (never deploys, in `.vercelignore`).** Added June 5, 2026. Holds the monetization strategy, the 001/002 rev-ops data analysis, the high-value-contacts + DM outreach tracker, and `revops/attendees/` (raw Zoom + enrichment lead CSV exports, PII). Read `revops/README.md` first when revisiting strategy/monetization/leads. One-line state: cheap cold top-of-funnel is proven; **revenue is the unbuilt bottleneck**; next move is a paid offer + conversion step + 3 pilots, not more audience.
- `.vercelignore`: excludes `mailer`, `email-signature.html`, brand-identity docs, `.claude`, `supabase`, `video-assets`, `tools`, `revops` from deploys.

## Registration / signup behavior

**RULE - rotating to a new session MUST also update the referral redirect.** The referral short link `/r/<code>` (`api/r.js`) redirects to one `CURRENT_SESSION` constant (currently `/sessions/006`). Whenever the site rotates to the next session, update `CURRENT_SESSION` in `api/r.js` in the SAME pass that updates `nav.js` (countdown label + `EVENT_START`/`EVENT_END` + CTA hrefs), the homepage session card, `/register`, the `/sessions` hub, `sitemap.xml`, and `llms.txt`, then `vercel --prod --yes`. This is not optional: skip it and every affiliate's `/r/<code>` link silently keeps sending people to the old session. Existing links keep working and auto-point to the new session, so referrers never need a new link. (Full system: the July 5 referral tracker entry.) **Also update the Zoom auto-registration env vars in the same pass:** set `ZOOM_WEBINAR_ID` (the new session's numeric webinar id, via `node tools/zoom-backfill.mjs --discover`) and `ZOOM_SOURCE_PREFIX` (the new session's `source` prefix, e.g. `episode-007`) in Vercel, then redeploy, or new signups keep registering onto the old webinar. (Full system: the July 9 Zoom auto-registration entry.)

**As of June 23, 2026: Episode 005 is dated, but the site still uses CAPTURE-ONLY signup, no Zoom redirect yet.** Homepage hero + closing forms POST to Supabase `signups` and show "You're in. We'll email you the Episode 005 details." `/register` POSTs to `ai4ntp_signups`, hidden source `episode-006-register-page`, and shows Session 006 capture-only copy (flipped to 006 on July 6, 2026). Leads still flow to Beehiiv via the `pg_net` trigger on the Supabase tables. `ZOOM_REGISTER_URL` has been deleted from homepage/register. **When the Zoom registration link exists, re-add the capture-then-redirect pattern** (define `ZOOM_REGISTER_URL`, restore the `setTimeout(... window.location.href ...)` after a 201/409, and flip the success copy back to "Taking you to Zoom…").

Prior pattern (for reference): from June 4, 2026 (Episode 003/004 Zoom live) forms captured to Supabase then redirected to the live Zoom registration so attendees got their unique join link. Same capture-then-redirect pattern was used for 002.

- The header countdown ticks to `EVENT_START` `2026-06-30T13:00:00-04:00` (Episode 005, June 30 1 PM Eastern) with `EVENT_END` `14:00`. It lives in **one place** (`nav.js`, used by the homepage too as of June 9, 2026).
- Cold-email / external traffic for 002 was fronted by Eventbrite (details in `sessions/002/CLAUDE.md`).

## Testimonials

A shared, self-contained component in **`/testimonials.js`** (namespaced `.ai4tm*`), injected on the homepage (between FAQ and the closing CTA) and `/register`. Rebuilt June 3, 2026 to a **MasterClass-style 1-up carousel**: a dark `ink` band with one quote at a time in a big Instrument-Serif-italic card, the person's headshot beside it, and an attribution block (name → their LinkedIn, `Title · Company` where company links to its site, location, and a `LinkedIn ↗` link). Everything is linked.

**Data lives in one place:** the `DATA` array at the top of `testimonials.js` (quote, name, title, company, companyUrl, linkedin, location, photo, initials). **To add/edit a testimonial, edit that array only** (no markup changes, no per-page edits). Quotes are transcribed verbatim from the LinkedIn screenshots in `testimonials/` (light cleanup). Headshots load from `/testimonials/headshots/<slug>.jpg`; if one is missing, the card shows the person's initials (`onerror` fallback), so it never looks broken.

Behavior: side prev/next arrows + dots, auto-advances every 7s (pauses on hover/focus, stops when tab hidden, off under `prefers-reduced-motion`), fade transition. **Mobile (≤820px):** stacks to a centered **circular** headshot + centered text (square photos would get head-cropped in a landscape box, so it uses a fixed circle), arrows hidden, and supports **touch swipe** (drag right-to-left = next). Fires `testimonial_engage` once when scrolled into view.

History (if revisiting): this replaced, in order, a single-slide screenshot carousel → a masonry "wall" → a 2-up screenshot carousel. The old inline `.tm-*` CSS still sits unused in `index.html` and `register/index.html` (dead, harmless; safe to strip in a cleanup pass).

## Analytics

GA4 (id `G-4YY783506Y`) + Vercel Web Analytics installed on: homepage, /register, /sessions/001, /sessions/002, /sessions/001/assess. Recap-page events: `book_30_min_click` (with `operator`), `youtube_external_click`, `deck_open_click`, `transcript_open`, `tool_click` (with `tool_name`). Form events: `signup_success` (register form), `home_signup_success` (homepage forms). Homepage funnel events: `faq_open` on `<details>` toggle, `testimonial_engage` now fires when the testimonials section scrolls into view (IntersectionObserver, homepage + `/register`), plus a generic `[data-cta]` click handler that fires the attribute value as the event name (`countdown_cta_click`, `nav_apply_guest_click`, `nav_save_seat_click`, `session_002_card_click`, `session_003_card_click`, `logistics_reserve_click`, `footer_save_seat_click`). The 002 recap page also fires `built_artifact_click` on the live-site / brand-kit links.

## Serverless API + env vars

`api/` holds Vercel serverless routes for the Episode 002 live brief-picker, both using the Supabase service-role key server-side:
- `api/synthesize.js`: POST `{ ideas: string[] }`, calls Claude Sonnet 4.6 to remix audience submissions into 5 candidate businesses, writes them to `ai4ntp_pulse_002_options`, flips state to `voting`. **No auth.**
- `api/state.js`: POST `{ phase }` (set phase / lock winner) or `{ reset: true }` (truncate all pulse data). **No auth** as of June 1, 2026 (host-auth removed at Justin's request).
- `api/beehiiv-sync.js`: **Supabase → Beehiiv subscriber sync** (added June 4, 2026). Goal: **every email captured anywhere on the site lands in Beehiiv.** Receives a Supabase `pg_net` trigger payload on INSERT, verifies header `x-webhook-secret` == `BEEHIIV_SYNC_SECRET`, then creates the Beehiiv subscription (`POST /v2/publications/{pub}/subscriptions`, `reactivate_existing:true`, `send_welcome_email:false`, `utm_source:website`). Covers **5 tables**, each tagged via the `Source` custom field (Beehiiv has no tags-on-create): `signups` + `ai4ntp_signups` → "Website Submission"; `ai4ntp_partner_apps` → "Partner Application"; `ai4ntp_pulse_001` → "Live Session 001"; `ai4ntp_pulse_002` → "Live Session 002" (`SOURCE_BY_TABLE` in the file). `mapRecord()` pulls `email`, splits `name` into First/Last (or uses `first_name`), and **skips malformed emails** (regex) so junk like "justin" never errors. Treats 409 / 422-"already" as success (idempotent). Beehiiv `Source`/`First Name`/`Last Name` custom fields must exist; publication ID needs the `pub_` prefix. **The Supabase side is a `pg_net` trigger** (dashboard "Database Webhooks" failed: `supabase_functions` schema not enabled): `create extension pg_net` + `security definer` function `public.beehiiv_sync_row()` that `net.http_post`s `{type:'INSERT', table, record:to_jsonb(NEW)}` to `/api/beehiiv-sync` with the secret header, plus `after insert` triggers on all 5 tables (`beehiiv_sync_signups`, `_register`, `_partner`, `_pulse001`, `_pulse002`). **Live + verified end-to-end June 4, 2026.**
- `api/beehiiv-backfill.js`: one-time paginated backfill (any of the 5 tables). `GET /api/beehiiv-backfill?secret=<BEEHIIV_SYNC_SECRET>&table=<table>&offset=0&limit=40`, loop until `hasMore:false`; `select=*` + shared `mapRecord`, skips no-email rows. Idempotent. (Ran June 4, 2026: 29 site signups + 27 partner/pulse subscribers synced.)

`supabase/` holds SQL: `ai4ntp_partner_apps.sql` and `ai4ntp_pulse_002.sql` (the three-table brief-picker schema). Run in the Supabase SQL editor; not deployed.

- `api/zoom-register.js`: **Supabase → Zoom webinar auto-registration** (added July 9, 2026). Receives a Supabase `pg_net` INSERT trigger payload on `ai4ntp_signups`, verifies header `x-webhook-secret` == `ZOOM_SYNC_SECRET`, source-gates on `ZOOM_SOURCE_PREFIX`, gets a Zoom **Server-to-Server OAuth** token (`POST https://zoom.us/oauth/token?grant_type=account_credentials`, Basic auth, cached), derives a first name from the email (form is email-only), and `POST /v2/webinars/{ZOOM_WEBINAR_ID}/registrants` so Zoom emails the unique join link + calendar invite. Idempotent (already-registered = success; ledger table `ai4ntp_zoom_registrations`). SQL + `pg_net` trigger in `supabase/ai4ntp_zoom.sql` (mirrors `beehiiv_sync_row()`). Backfill/discovery: `tools/zoom-backfill.mjs`. Full details: the July 9 work-log entry.

**Required Vercel env vars (Production):** `SUPABASE_SERVICE_ROLE_KEY` and `ANTHROPIC_API_KEY` (brief-picker); `BEEHIIV_API_KEY`, `BEEHIIV_PUBLICATION_ID` (`pub_…`), and `BEEHIIV_SYNC_SECRET` (Beehiiv sync); `ZOOM_ACCOUNT_ID`, `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, `ZOOM_WEBINAR_ID`, `ZOOM_SOURCE_PREFIX`, `ZOOM_SYNC_SECRET`, `CRON_SECRET` (Zoom auto-registration + paced backfill cron). Env-var changes require a fresh deploy to take effect. (`HOST_SECRET` is no longer used.) `.vercelignore` also excludes `*.bak`.

## Deploy

Run `vercel --prod --yes` from `/Users/justinnovak/Desktop/ai4ntp/ai4ntp`. Env-var changes only take effect on a fresh deploy.
