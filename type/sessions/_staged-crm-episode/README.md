# Staged: CRM crash-course episode

This is the drafted "The Agentic AI CRM that changed Justin's life" episode (GoHighLevel affiliate,
Justin solo). It was originally built as `/sessions/004` but is being **staged for a future episode**
while 004 was repurposed for the "7 AI tools you've never heard of" session (Alec + Justin, June 18, 2026).

Kept here (and in `.vercelignore`, so it does not deploy or get indexed) until it's slotted into a real
episode number. To revive: move `index.html` + `og.json` + `og-image.jpg` + `images/` into the target
`sessions/00X/` folder, update the episode number / canonical / OG paths, and wire it into the site like
any new session.
